## 0.3.1 — 修掉「打开书回到开头」的回归

0.3.0 把文件夹书的章节改成「自动挑文件名像章节的那些」，命名不规范的书会被挑得只剩一两章，
已保存的章序号被截断到第 1 章 —— 表现就是每次打开都从头开始、书看起来是空的。

### 修复

- 文件夹书**没有手动配置过章节时，改为整文件夹都算章节**（0 字节占位文件仍然自动跳过），
  不再按文件名自动挑。少给几章会让进度被截断，宁可多几篇设定稿在目录里。
- 想排除设定稿 / 世界观这类非章节文件：书籍详情 →「重新选择章节」，选一次会记住。

### 兼容

- 只影响「没有手动配置过章节」的文件夹书；已经手动选过章节的书不受影响。
- 需要说明：这本文件夹书如果在 0.3.0 里被打开过，进度可能已经被覆盖成第 1 章，
  升级后要重新翻回原来的位置（书签不受影响，还在原处）。
