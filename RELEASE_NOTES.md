## 0.3.2 — 手机上不再被困在阅读器里

移动端打开阅读器后切回别的笔记，界面仍然是沉浸模式：标题栏和底部导航栏全被藏了，
既关不掉阅读器，也回不到正常界面。

### 修复

- **沉浸模式跟随活动视图**：只有当阅读器是当前正在看的那个视图时才隐藏标题栏/导航栏，
  切到别的笔记立刻恢复正常界面，切回来再自动沉浸（原来只在视图关闭时才摘掉）。
- **操作栏新增「退出阅读器」**：直接关掉阅读器这个视图，手机上有了明确的出口。
- **沉浸模式下右上角多一个很淡的 ✕**：点它唤出操作栏（里面有退出阅读器、沉浸开关、换书）。
  沉浸模式会把导航栏藏掉，总得留个能点到的地方。
- 命令面板新增「关闭阅读器」。

### 兼容

- 沉浸开关的默认值不变（开）；不喜欢全屏阅读的可以在操作栏里关掉，会记住。
