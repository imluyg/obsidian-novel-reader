## 0.3.0 — 书架：像书架了，还能置顶、改名、批量管

书架以前只是个能排序的历史列表，现在是一本正经的书架：有封面、有分类、能看到读到哪了，
还多了一个常驻的书架视图，不用先打开阅读器也能进。

### 新增

- **独立书架视图**：侧边栏书架图标或命令面板「打开书架」打开，和阅读器平级，库里有变动会自动刷新。
  阅读中从底部操作栏点「书架」仍是弹窗，读完接着看不被打断。
- **网格卡片视图**：书名首字生成封面色块，手机三列、桌面自动加列；和列表一键切换，会记住上次的选择
  （设置里也能改默认值）。
- **分类标签**：全部 / 在读 / 收藏 / 未读 / 读完，标签上直接带计数，书多了不用靠搜索框。
- **看到读到哪**：每本书显示进度条、`已读 x%`、`读到 第四章 雨夜`、书签条数和「3 天前」这类相对时间。
  老版本的进度记录没有章节标题，翻一次页就会补上。
- **置顶与显示名**：常用的书置顶到书架最前；文件名不好看可以起一个只在书架里生效的别名，不动文件本身。
- **书籍详情**：点 ⓘ 看进度、章节位置、书签数、路径（点击复制），置顶 / 改名 / 重置进度 / 移除都在这一处。
- **批量操作**：点「批量」进入多选，一次收藏、取消收藏或移除多本书。
- 桌面端详情里多一个「在文件管理器中显示」，方便直接去改原稿。

### 优化

- 书架与选书器共用一套列表实现，弹窗和常驻视图行为完全一致。
- 浏览全库弹窗不再依赖阅读器：书架视图里也能直接加书，加完自动刷新。
- 打开一本书统一走「没有阅读器就先开一个」，从书架点书不会再出现没反应的情况。

### 兼容

- 老数据没有收藏表、置顶表、章节标题字段，升级后按空表处理：
  已有的阅读进度、书签、排版设置都不受影响，升级后书架照常显示。
